<%= form_for(@element,  remote: true) do |f| %>
  <% if @element.errors.any? %>
    <div id="alert alert-block alert-error">
      <h2><%= pluralize(@element.errors.count, "error") %> prohibited this element from being saved:</h2>

      <ul>
      <% @element.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


 <div class="control-group <%= 'error' if @element.errors[:nombre].present? %>">
        <label for="nombre" class="control-label">Nombre</label>
        <div class="controls">
            <%= f.text_field :nombre, :maxlength => "100" %>
        </div>
    </div>

     <div class="control-group <%= 'error' if @element.errors[:descripcion].present? %>">
        <label for="descripcioin" class="control-label">Descripcion</label>
        <div class="controls">
            <%= f.text_field :descripcion, :maxlength => "100" %>
        </div>
    </div>

  
 <div class="actions">
  <%= f.submit yield(:boton), :class => "btn btn-primary"%>
          
 <%= link_to :back, :remote => true, :type => "reset", :class => "btn" do%>
        <i class="icon-chevron-left"></i>Cancelar
    <% end %>  
</div>
<% end %>
