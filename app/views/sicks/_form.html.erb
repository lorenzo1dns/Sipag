<%= form_for(@sick) do |f| %>
  <%= render 'shared/error_messages_sick' %>
     <div class="row-fluid">
                      <div class="span6">
                         <%= f.label :name %>
                         <%= f.text_field :name, required: 'True'  %>  
                      </div>
                      <div class="span6">
                         <%= f.label :familia %>
                          <%= f.text_field :familia %>
                      </div> 
      </div>
      <div class="row-fluid">
                  <div class="span6">
                     <%= f.label :descripcion %>
                  <%= f.text_area :descripcion, size: "60x6"%>  
                  </div>
                  <div class="span6">
                    <%= f.label "Daño" %>    
                  <%= f.text_area :danio, size: "60x6" %>
                  </div>
      </div>
     
    
    <%= f.file_field :avatar %>
  
    <% if yield(:boton) == 'Salvar Cambios' %>
        </div>
        <div class="modal-footer">
                    <a class="btn" data-dismiss="modal" aria-hidden="true">Cancelar</a>
                   <%= f.submit yield(:boton), class: "btn btn-primary" %>
               </div>
      <% else %>
          <%= f.submit yield(:boton), class: "btn btn-primary" %>
      <% end  %>
  
 
  
<% end %>
