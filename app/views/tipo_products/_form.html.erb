<%= form_for(@tipo_product) do |f| %>
  <% if @tipo_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_product.errors.count, "error") %> prohibited this tipo_product from being saved:</h2>

      <ul>
      <% @tipo_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
